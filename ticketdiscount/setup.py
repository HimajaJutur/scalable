from setuptools import setup, find_packages

setup(
    name="ticketdiscount",
    version="0.1.0",
    description="A simple seat-based bulk discount library",
    author="Himaja",
    packages=find_packages(),
    install_requires=[],
)